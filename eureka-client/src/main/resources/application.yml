# 指定服务的注册中心
eureka:
  client:
    serviceUrl:
#    注册中心地址
      defaultZone: http://localhost:8761/eureka/

# 指定该服务端口
server:
  port: 8762

# 指定该服务名称(也是显示在注册中心里的名称)
spring:
  application:
    name: service-hi
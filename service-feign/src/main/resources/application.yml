eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8765
spring:
  application:
    name: service-feign

#Feign自带断路器  默认是关闭的,需要手动打开
feign:
  hystrix:
    enabled: true